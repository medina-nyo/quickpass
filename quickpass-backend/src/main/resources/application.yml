spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quickpass?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: quickpass_user
    password: QuickPass@2025
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        show_sql: true
    open-in-view: false

  messages:
    basename: messages
    encoding: UTF-8

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
